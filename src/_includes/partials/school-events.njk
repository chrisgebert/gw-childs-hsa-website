{% for event in schoolCalendar | futureEvents %}
- {{ event.date | dateFormat("MMMM d, yyyy") }}: {{ event.event }} <br/>
{% endfor %}

{% if (schoolCalendar | futureEvents).length === 0 %}
    No upcoming events in the next 3 months.
{% endif %}